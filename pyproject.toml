[build-system]
requires = ["setuptools>=61.0.0,<69.3.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python_layout"
version = "0.1.0"
requires-python = ">= 3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["python_layout*"]